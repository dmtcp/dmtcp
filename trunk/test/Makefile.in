@SET_MAKE@
LN_S=@LN_S@
CC=@CC@
CXX=@CXX@
CFLAGS=@CFLAGS@
CFLAGS+=-Wl,--export-dynamic
CXXFLAGS=@CXXFLAGS@
HAS_READLINE=@HAS_READLINE@

TESTS=$(basename $(wildcard *.c *.cpp echoserver/*.c))

tests: $(TESTS)

clean distclean:
	rm -f $(TESTS)

readline: readline.c
ifeq ($(HAS_READLINE),yes)
	$(CC) -o $@ $< $(CFLAGS) -lreadline -lhistory
else
	#required libraries missing, skipping test
endif

%: %.c
	$(CC) -o $@ $< $(CFLAGS)

%: %.cpp
	$(CXX) -o $@ $< $(CXXFLAGS)


